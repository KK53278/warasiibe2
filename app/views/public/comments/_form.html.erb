<%= form_with(model:[product, comment], url:public_product_comments_path(@product), local: true) do |f| %>
  <%= f.text_area :comment, rows:'5', placeholder: "", class: "w-100" %>
  <%= f.submit "送信", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>